apiVersion: v1
kind: Secret
metadata:
  name: ai-incident-copilot-secret
  labels:
    app.kubernetes.io/name: ai-incident-copilot
type: Opaque
stringData:
  # В production эти значения должны приходить из внешнего secret-management
  # или CI/CD, а не использоваться в виде примера как есть.
  POSTGRES_USER: incident
  POSTGRES_PASSWORD: change-me
